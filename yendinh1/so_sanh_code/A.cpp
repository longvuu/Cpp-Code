#include <bits/stdc++.h>
using namespace std;

int main() {
    // Đọc từ file input
    freopen("A.inp", "r", stdin);
    // Ghi ra file output (thuật toán chính)
    freopen("A.out", "w", stdout);
    
    // =================== CODE THUẬT TOÁN CHÍNH ===================
    // Viết thuật toán tối ưu của bạn ở đây
    
    int a, b;
    cin >> a >> b;
    cout << a + b << "\n";
    
    // ============================================================
    
    return 0;
}
